_schema-version: "3.2"
ID: ui5deploy
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: ui5deploy-destination-content
  type: com.sap.application.content
  requires:
  - name: ui5deploy-destination-service
    parameters:
      content-target: true
  - name: ui5deploy-repo-host
    parameters:
      service-key:
        name: ui5deploy-repo-host-key
  - name: ui5deploy-uaa
    parameters:
      service-key:
        name: ui5deploy-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: ui5deploy_html_repo_host
          ServiceInstanceName: ui5deploy-html5-srv
          ServiceKeyName: ui5deploy-repo-host-key
          sap.cloud.service: ui5deploy
        - Authentication: OAuth2UserTokenExchange
          Name: ui5deploy_uaa
          ServiceInstanceName: ui5deploy-xsuaa-srv
          ServiceKeyName: ui5deploy-uaa-key
          sap.cloud.service: ui5deploy
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: ui5deploy-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: ui5deploy-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - ui5deploy.zip
      name: ui5deploy
      target-path: resources/
- name: ui5deploy
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: ui5deploy-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: ui5deploy-destination-service
    service-plan: lite
- name: ui5deploy-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: ui5deploy-xsuaa-srv
    service-plan: application
- name: ui5deploy-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: ui5deploy-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
