const { MULTITENANCY, TOGGLES, EXTENSIBILITY } = require('../../constants').OPTIONS

module.exports = class MtxTemplate extends require('../templateBase') {

    getDependencies() {
        return [MULTITENANCY, TOGGLES, EXTENSIBILITY]
    }
}
