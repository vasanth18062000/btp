#Step 1 
Create a New Project 
    Go to Menu -> file -> New Projet From Template -> Select CAP Project - > Select Node.js
#Step 2 - Create a file with extension of .cds and inside db Folder - > This File used to create Entity
#Step 3 - Create DATA Folder inside DB Folder 
#Step 4 - Create Entity - 

 EXAMPLE :
 
using { Currency, managed, sap } from '@sap/cds/common';
namespace sap.capire.bookshop; 

entity Books : managed { 
  key ID : Integer;
  title  : localized String(111);
  descr  : localized String(1111);
  author : Association to Authors;
  genre  : Association to Genres;
  stock  : Integer;
  price  : Decimal(9,2);
  currency : Currency;
}
entity Authors : managed { 
  key ID : Integer;
  name   : String(111);
  books  : Association to many Books on books.author = $self;
}

Note - "cds add samples" - this command used to create a sample entity and data and service 
in project  it will create book entity as  sample Entity

# Step 5 - Deployed to Databases Automatically

DB Created Conformation we can see the log - "successfully deployed to in-memory database."

$" No service definitions found in loaded models.
    Waiting for some to arrive..."$ - this means no service written for model
#Step 6 - create sample Data
Create Data Sample Data as .csv file inside data folder in DB Folder

** .CSV file Name Like - namespace name - entity name.csv



